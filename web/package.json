{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate && cp -r ../data/dist/* .output/public/",
    "preview": "trap 'rm -f .output/public/investment' EXIT; cd .output/public && rm -rf investment && ln -s . investment && cd ../.. && nuxt preview"
  },
  "dependencies": {
    "nuxt": "^4.4.8",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.1"
  }
}